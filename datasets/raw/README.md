# raw/ — download target (intentionally empty)

`fetch_dataset.py` writes the downloaded ESC/VSC files here. This directory ships
empty because the artifact does not redistribute the authors' data. Run:

    python datasets/fetch_dataset.py

to populate it from the authoritative source (see ../DATASETS.md).
